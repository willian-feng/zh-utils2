import { spell } from './cfg/spell';

const chars2zh = (str) => {
  // for (let value, key of spell) {
  //   if (-1 !== spell[key].indexOf(str)) {
  //     return value;
  //   }
  // }
  return false;
}

export default chars2zh;
